let test = `Shailendra`;
console.log(test);

let number = 2;
let text = "";

while (number !== Infinity) {
  number *= number;
  text = text + number + " "; // Add a space after each number
}

document.write(text + "\n"); // Add a newline at the end of the text
